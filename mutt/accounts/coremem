# :hook folder-hook "$HOME/Mail/coremem.alex" source "$HOME/.mutt/accounts/coremem"
# :hook mailboxes "$HOME/Mail/coremem.alex/INBOX"

set folder="$HOME/Mail/coremem.alex"
set spoolfile=+INBOX
set record=!
set postponed=!.Drafts

set from=%coremem.email%

set use_envelope_from=yes
set envelope_from_address=%coremem.email%

alias me %coremem.email% (%name%)
alternates '^alex(\\+[a-zA-Z0-9-]+)?@coremem\.com$'

my_hdr Organization: coreMem Limited

macro index,pager S "<save-message>!.Spam<enter>" "File as Spam"
