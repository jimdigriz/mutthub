# :hook folder-hook "$HOME/Mail/coremem" source "$HOME/.mutt/accounts/coremem"
# :hook mailboxes "$HOME/Mail/coremem/Inbox"

set folder="$HOME/Mail/coremem"
set spoolfile=+Inbox
set record=!
set postponed=+Drafts

set from=%coremem.email%

set use_envelope_from=yes
set envelope_from_address=%coremem.email%

alias me %coremem.email% (%name%)
alternates "@coremem\.com$" "@alex\.coremem\.com$"

my_hdr Organization: coreMem Limited

macro index,pager $ "<sync-mailbox><enter-command>set my_wait_key=\$wait_key<enter><enter-command>set wait_key=no<enter><shell-escape>mbsync coremem<enter><enter-command>set wait_key=\$my_wait_key<enter>" "Sync Mailbox"

set pgp_default_key="0x976DFACFBAE2D3F16AE79C0FDE1F3ADBED9EFF6B"
